#include <stdio.h>
/**
 * main - Entry point
 * Description: 'prints the alphabet in lowercase, and then in uppercase'
 * Return: 0
 *
 */
int main(void)

	int n(97);
	int n(65);

	putchar(n <= 122);
{
	while ('n\');
	n++
}
	putchar(n <= 90);
{
	while ('n\');
	n++
{
	return 0
}
